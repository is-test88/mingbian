# 明辨 3.2.0｜本次优化说明

日期：2026-09-05。基于仓库 3.1.0 实际内容整理的 3.2.0 发布版本。

## 一、判断与取舍

原版最值得保留的不是某个提问模板，而是完整的工作原则：先读材料、先给判断、一问一辨、每答重算、从方向下沉到必要落法，最终形成《明辨录》。这些已具备明确定位，无需再改造成“更严厉的审查器”。

这次优化聚焦三个问题：规则能否在真实边界场景下自洽，讨论能否以适当成本结束，文字规则能否与可选状态工具一致。

保持名称“明辨”、副标题“方案推演与成事之法”、五步章法、六层辨题、自然触发和最终产物不变。新增能力都服务于原有体验，普通用户无需使用脚本、选择模式或填表。

## 二、主要问题与修改

### 1. 取舍问题与必要信息不能共用同一门槛

原版 SKILL 与 interview-protocol 要求每个问题都通过五关，其中包括“至少存在两个合理答案”。这对防止无效取舍题有帮助，但某个不可延后的日期只有一个事实答案；一项发送操作也可能没有两条合理路径，却仍需具体授权。

新版区分取舍、事实补充和授权确认。五关仅约束取舍题；不可自查且关乎行动的事实可以补问，具体授权按边界处理。三者共享一轮一个待响应事项的额度，不借分类之名多问。

### 2. 深入程度应由本轮要完成的事决定

原版强调最终可执行、可交接、可验收，这个方向正确；但若用户只想判断要不要做，直接要求写清完整实施细节会增加负担。

新版从请求自动识别方向、试点或执行深度。方向交付必须明确机制、关键边界和验证路径；试点必须明确范围、投入边界、责任、接管与验收；执行交付继续下沉到输入输出、流程、交接与例外。不是降低质量，而是不把下一阶段的全部问题提前塞进本轮。

同时明确“方向就绪”不等于“执行就绪”。阶段变浅也不能豁免实际动作的风险和授权要求。

### 3. 从原则式提醒改为可操作的对话规则

原版已经提出先判后问、动态重算和正确停止。新版补充了实际分叉的处理：用户只表达倾向、反驳推荐、纠正上游、没有工具、查不到事实、不想再回答、要求完整方案等。

重要推荐说明什么证据会使判断改变；先公平比较最强替代路径，而非只支持原想法。连续两轮没有新增判断或方案变化时，改变验证方式或交付阶段底稿，不继续原地追问。

正常交付不强制追加“确认纪要”的一轮；只有用户原本要求确认关口或实际动作需要授权时，才保留确认。

### 4. 主文件独立运行，减少重复加载

原版要求每次启动同时读取 SKILL.md 与完整 interview-protocol.md，两份文件有较多原则重复。新版主文件独立可用，将复杂分歧、状态操作、示例与维护评测改为按需读取。

原主文件与必读协议合计 29,433 字节；新主文件 13,360 字节、169 行。这里比较的是必读文本文件字节，不是 Token、响应时延或实测效果，不能据此宣称模型质量提高了某个比例。

这一组织方式参考 Agent Skills 规范关于主文件与按需参考的渐进披露建议，但本包没有声称获得官方认证或在全部客户端完成兼容性验证。

### 5. 状态清零不能自动代表方案完整

原脚本拒绝空 branches，但产品允许零问题结束；can_confirm 只检查已登记分支是否处理完，整体交付质量需要代理另外检查。

新版允许空图，同时将 graph_resolved、review_current 与 can_confirm 区分。需要确认归档时，必须关联当前《明辨录》文本快照、六层复核依据与下一步。后续变更使旧复核失效。

这只把复核过程显式化，不能自动证明方案正确。脚本不会识别虚假证据或判断业务因果；填写六项 pass 也不代表语义上已经通过。

### 6. 保存结论时保留来源性质与适用条件

原版主要用 decision/fact 和自然语言容纳默认、假设与授权。新版分别保留已核实事实、用户陈述、明确决策、默认、假设、后议与授权记录。默认须有低风险、可逆边界与改回办法；假设须有验证和失败处理；后议不能满足下游硬前置。

这既方便恢复，也防止把“暂按如此”“我倾向于”读成“用户已批准”。脚本仅验证这些声明完整，仍由代理核实其含义。

### 7. 修正状态工具的实际可靠性边界

原版已有原子写入和版本计数，但未用版本号阻止并发覆盖；方案哈希保存后没有持续回读；上游重开后在同批重锁，也可能让旧下游继续保持 locked。

新版增加 expected_revision、本地排他锁、来源变化检测和下游自动重审；同时支持重新绑定材料、重开会话、导出和显式迁移。旧版 schema 2 文件先逐字节备份，旧答案保留供核对，不自动推定事实或授权类别。

这些是本地辅助工具的防误用措施，不等同于在线多人协作、不可篡改审计、身份认证或生产系统权限控制。

## 三、实际交付

主规则、推演协议、立意、产品介绍、使用说明、展示提示与版本说明已同步；新增对话示例、灵活《明辨录》模板、离线示例与评估素材。原 MIT 许可证逐字节保留，校验后的 Git blob 与原仓库一致。

本包不是一份“后续应该怎么改”的建议清单，而是包含完整文件的替换目录。来源基线的 commit SHA、Git tree SHA 与原始文件 blob 记录在 [SOURCE_BASE.json](SOURCE_BASE.json)。

## 四、验证结果与未验证项

**已执行通过 72 项自动化测试**：38 项纯状态测试，29 项命令行测试，5 项结构与合成示例测试；失败、错误、跳过均为 0。命令行测试按不重复批次执行，全部覆盖。附原始日志及用例 ID，见 [测试结果](validation/TEST_RESULTS.json)。

离线示例验证了校验图、新建、记录决策与事实、交付复核、导出和确认归档流程。所有示例决策与确认都是合成测试设定，不是真实用户授权。

仓库专用静态检查通过，覆盖文件、引用、必要格式、JSON、声明的 Python 3.10 语法约束。执行环境为 Linux / Python 3.13；没有运行 Windows、macOS 或网络文件系统测试矩阵。

**19 个触发／不触发输入与 26 个行为场景已经编写，但尚未在目标模型上运行。** 对话示例也是作者编写的范例，不能当作真实模型输出。因此本版没有“准确率提升”“效果超过某模型”等未经测量的结论。实际模型与客户端中的讨论体验，按 [验成规](references/evaluation.md) 验证。

没有进行生产部署或真实业务操作；GitHub 提交仅发布本次 Skill 文件与配套测试、示例和说明。

## 五、使用与升级

先备份旧 Skill 目录，再整包替换 `mingbian/`，不要只更新一个 Python 文件而继续沿用旧协议说明。普通对话不依赖 Python；没有使用过明辨簿的用户无需迁移任何状态。

使用过旧会话脚本的维护者，按 [状态协议](references/session-state.md) 显式迁移。修改命令需要 expected_revision；lock 需要来源类别和来源；confirm 前需要 review。不要用示例中的确认文字替代真实用户决定。

入口不变：

> 用明辨来跟我讨论一下 XXXXX。

给出大概背景与想法后，由明辨引导后续讨论。

## 来源与参考

- 原仓库：https://github.com/is-test88/mingbian
- 检查基线：3.1.0；commit `29d2da80d12d88f1cf5918b45b58c33f8962b5f4`，Git tree `453aaaa7c2c21d996c4fb2658a86312872efd28c`。
- 规则证据：原 SKILL.md、references/interview-protocol.md、references/session-state.md。
- 实现证据：原 scripts/mingbian_session.py、scripts/test_mingbian_session.py。
- 格式参考：https://agentskills.io/specification （检查日期：2026-09-05）。
